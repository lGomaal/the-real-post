import { NextAndPreviuosOneTitleStepperDirective } from './next-and-previuos-one-title-stepper.directive';

describe('NextAndPreviuosOneTitleStepperDirective', () => {
  it('should create an instance', () => {
    const directive = new NextAndPreviuosOneTitleStepperDirective();
    expect(directive).toBeTruthy();
  });
});
